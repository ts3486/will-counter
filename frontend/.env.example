# Frontend Environment Variables
# These variables are safe to expose in the frontend bundle
# DO NOT put sensitive keys here - they will be visible to users

# Supabase Configuration
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Auth0 Configuration
EXPO_PUBLIC_AUTH0_DOMAIN=your-domain.auth0.com
EXPO_PUBLIC_AUTH0_CLIENT_ID=your-auth0-client-id
EXPO_PUBLIC_AUTH0_AUDIENCE=your-auth0-audience

# API Configuration
EXPO_PUBLIC_API_BASE_URL=http://localhost:8080

# CRITICAL SECURITY NOTES:
# ========================
# 1. NEVER include SUPABASE_SERVICE_ROLE_KEY in frontend environment variables
# 2. Service role keys should ONLY exist on secure backend servers
# 3. All variables prefixed with EXPO_PUBLIC_ are bundled into the app
# 4. Use Auth0 JWT tokens for authentication with Supabase RLS policies
# 5. Anon keys are safe to expose - they respect RLS policies