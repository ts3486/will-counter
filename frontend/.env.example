# Frontend Environment Variables
# These variables are safe to expose in the frontend bundle
# DO NOT put sensitive keys here - they will be visible to users

# Supabase Configuration
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Auth0 Configuration
EXPO_PUBLIC_AUTH0_DOMAIN=your-domain.auth0.com
EXPO_PUBLIC_AUTH0_CLIENT_ID=your-auth0-client-id
EXPO_PUBLIC_AUTH0_AUDIENCE=your-auth0-audience

# API Configuration
EXPO_PUBLIC_API_BASE_URL=http://localhost:8080

# SECURITY NOTES:
# ===============
# 1. All variables prefixed with EXPO_PUBLIC_ are bundled into the app and visible to users
# 2. NEVER include service role keys, API secrets, or sensitive credentials here
# 3. Use the secure backend API for all database operations  
# 4. Supabase anon key is safe to expose (respects RLS policies) but not used in secure mode
# 5. Auth0 access tokens are used for backend API authentication