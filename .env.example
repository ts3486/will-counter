# Environment Configuration for Will Counter API
# SECURITY: Never commit actual secrets to version control

# Supabase Configuration
# Get these from your Supabase project settings
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Auth0 Configuration
# Get these from your Auth0 application settings
AUTH0_DOMAIN=your-domain.auth0.com
AUTH0_CLIENT_ID=your-client-id-here
AUTH0_AUDIENCE=https://your-api-identifier

# Database Configuration (for API)
# Use a strong password and restrict database access to necessary IPs only
DATABASE_URL=postgresql://username:password@localhost:5432/will_counter

# Security Notes:
# 1. Use strong, unique passwords for database access
# 2. Rotate secrets regularly (at least every 90 days)
# 3. Restrict database access to specific IP addresses
# 4. Use environment-specific secrets (dev, staging, prod)
# 5. Monitor for unauthorized access in Auth0 and Supabase dashboards
# 6. Keep all dependencies up to date for security patches